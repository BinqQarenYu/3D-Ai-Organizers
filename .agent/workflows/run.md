---
description: How to quickly run the 3D AI Organizers application with all fixes applied.
---

This workflow automates the stabilization and startup of the 3D AI Organizers app. It kills existing processes on the dev ports, ensures dependencies are met, and launches the environment.

// turbo
1. Run the startup command:
```powershell
powershell -ExecutionPolicy Bypass -File ./run.ps1
```
